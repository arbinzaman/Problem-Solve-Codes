#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a,b,c;
    cin >> a;
    b = a << 3;  /*left shit */
    c= b >> 3;  /*right shit*/
    cout << b<< " " << c;


    /** 
    *!Shift is a bite wise operator , shift works in binary "2"
    *!Bitwise operators works binary number system it shifts binary number
    *!In Right Shift numbers reduced in right
    *!If we want to know first digit of the odd number in binary we can use bitwise & with 1

    */

}
